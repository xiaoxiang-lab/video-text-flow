# 关卡定义（0-5）

每个关卡 = 交付物 + 三层校验 + 一次用户决策。**关卡完整，禁止合并/跳过。**

## 确认机制（决策摘要，不是全文阅读）

**用户确认 = 只做「程序/AI 做不了的决策」，不做阅读。** 每关确认输出 30 秒决策摘要：

```
✅ 已过：程序校验 + 子 agent 复核（详情在文件复核记录里，用户不用读）
🔴 需决策：只有这些要用户拍板（每项给 AI 建议）
🟡 提示：不需决策的制作期注意项
规则：无 🔴 项 = 摘要一行「已过」；用户说「过」或对决策项回答即通过
```

哪些进 🔴（需决策）：信息取舍（重复/删留/精简）、主观偏好、超出 AI 能力的判断。
哪些不进（已过）：程序+子 agent 全部判定通过的项目、制作期技术提示。

## 关卡 0 · 立场确认（对话，无文件）

- 程序：—
- 子 agent：AI 复述素材立场一句话
- 用户决策：认同 → 提炼主张；不认同 → 走立场重写（human-writing，保留事实换立场，以「我」为主语）；直接给立场 → 路径 2（从零写）

## 关卡 1 · 立场重写稿（01-立场重写稿.md）

- 程序：`check_rewrite.py`（冒号/破折号/排比/长列举/关键事实保留）
- 子 agent：事实未变、立场已换、无杜撰、口播可念（受约束：清单自拉+结构化输出）→ **核对表落盘**（如 `.work/checklist-01.md` 或产物旁，明细级：逐项判定 + 异常处置）
- 用户决策：✅ 立场对不对、事实丢没丢
- 规则版本记录：复核记录内标注

## 关卡 2 · 分镜稿 + 标题 + 风格（02-分镜稿.md）

**⚠️ 风格确认是独立 question 步骤（不与其他问题混弹）**：在分镜稿确认前单独弹一次，显眼标注「风格 = 正片视觉语言 + 封面皮肤，定了之后拆镜和封面都用它」，给 2-3 个候选（chupian-vox 风格库）用户定，不默认。风格确认后：拆镜按该风格语言写 video_prompt；封面按该风格皮肤替换【固定视觉语言】。

- 程序：`check_storyboard.py`（镜数/字数/残句/条件从句/标注/设问连续）
- 子 agent：一句一知识点、视觉落点、类型豁免裁决（定义/论证链）、来源真实性 → **核对表落盘**（明细级）
- 用户决策（三个独立 question，顺序：风格 → 标题三选一 → 分镜稿决策摘要）：
  1. 风格确认（独立弹窗，显眼标注）
  2. 标题三选一（独立弹窗）
  3. 分镜稿（决策摘要模式：只需决策 🔴 项）

## 关卡 3 · 配音定稿（03-配音定稿.md）

- 程序：`check_storyboard.py`（复用——定稿 = hook + 分镜拼接）
- 子 agent：hook 立场与正文一致、不杜撰、与第一镜无重叠 → **核对表落盘**（明细级）
- 用户决策：✅ 定稿全文（决策摘要：通常无 🔴 项）

## 关卡 4 · 封面（04-封面出图提示词.md）

- 程序：`check_cover.py`（排版内置/双模式/禁止词/皮肤整段替换）
- 子 agent：模板结构合规、皮肤替换完整、标题逐字 → **核对表落盘**（明细级）
- 用户决策：✅ 封面（明示 `封面皮肤：<风格名>`，用户可换；生成后核验意象是否贴题）

## 关卡 5 · 拆镜作业单（05-拆镜作业单.md）

- 程序：`check_jobcard.py`（镜数/时长档合法/超长句档位充足/参考图数量/image_prompt 与 ✅ 镜对应/贯穿装置节/复核块）+ `check_chain` 03→05 narration 逐字链（H 行=03 hook，其余=03 正文）
- 子 agent：核对表（narration↔video_prompt 逐镜对齐，40 镜全量判定，异常<20% 打补丁）→ **核对表落盘**（明细级，先例：`核对表-jobcard-review-05.md`）
- 用户决策：✅ 镜头/时长/参考图/贯穿装置（决策摘要：AI 给建议 + 判断标准，用户只拍板）

**关卡 5 判断标准（固化，AI 拆镜与给建议时使用，不得重新发明）**：

1. **时长档**：字数 → 档位换算 ≤18 字→4s、19-27→6s、28-36→8s、>36→10s（读速 ≈4.5 字/s，LESSONS D1）；档位是「念得完」的最小档，不压缩（压 = 念不完，变速 >5% 不自然）；>45 字应在分镜稿阶段就拆句（check_storyboard 预警）。
2. **拆句**：narration 边界内允许拆句（23→23a/23b），拼接须逐字还原 03；拆点是切分不是改写。
3. **参考图**（vox-prompts 四条规则，命中任一条才配）：①贯穿主体需锁造型 ②多具名元素空间关系 ③必须准确的文字/数字 ④结构性镜头（开场/核心揭示/结尾）。典型 3-5 张，不是每镜；同源复用镜（提示词显式引用上一镜几何）不配。
4. **贯穿装置**（判断标准三条）：①跨镜一致性（反复出现的实体）②立场视觉化（主张的最短映射）③皮肤同源（不混入其他风格）。给用户建议时必须带这三条判断标准，不把「要不要」裸抛给用户。

## 通用硬规则

1. 关卡完整性：每关独立交付独立确认
2. 立场一致性：hook/标题/封面/拆镜立场 = 关卡 0 确认的立场
3. narration 边界：VOX 逐字切分定稿全文，立场问题在源头解决
4. 交付物带复核记录块（无标记 = 未完成）
5. **子 agent 复核必须落盘明细级核对表（2026-08-15 起，全关卡通用）**：每关子 agent 复核产出独立核对表文件（逐项判定 + 异常处置），放产物同目录或项目 `.work/`（关卡 5 先例 `核对表-jobcard-review-05.md`，阶段 2 先例 `.work/review-design-核对表.md`）。摘要级（只在产物复核块里写几句结论）不是本规则允许的完成形态；01-04 是规则确立前的历史产物，维持摘要级不追溯，新文案/新话题一律明细级
6. 规则更新 → 旧产物回扫重查（按新规则版本）
