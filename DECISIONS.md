# DECISIONS.md · 关键决策记录（跨对话）

> 格式：日期 | 决策 | 理由 | 影响。新对话先读，避免重复讨论。

## 2026-08-15

| 决策 | 理由 | 影响 |
|---|---|---|
| **能程序化就不要大模型**：确定性规则（字数/镜数/冒号/排比/标注/禁止词）用程序校验器，语义规则用受约束子 agent，用户只做决策 | LLM 执行规则 = 概率性重新解释（有方差=漂移）；程序 = 翻译一次执行 N 次（零方差） | checks/ 程序层 + 子 agent 语义层 + 用户决策层 |
| **校验器自身被测试覆盖**（TDD） | 程序也有 bug，校验器写错 = 假通过比没有更糟 | checks/tests 36 例 |
| **决策摘要确认**：用户只读 🔴 需决策项，不做全文阅读 | 39 镜全文确认无意义（用户不会读），确认 = 决策不是阅读 | 关卡确认模式 |
| **独立风格确认弹窗**（不与其他问题混弹） | 混弹时风格被带过（实测：问过但用户没意识到） | 关卡 2 内独立 question |
| **封面模板固化**：只填标题+换皮肤，核心创作任务原文不动 | 实测：锁死装置的具体化版本效果差，模板原文给模型自由推导空间更好 | visual-cover 模板 |
| **立场双入口**：路径 1 素材立场≠用户立场时 human-writing 重写（保留事实换立场） | 立场必须是用户的，不是素材的（冲突理论） | 关卡 0-1 机制 |
| **narration 边界**：VOX 逐字切分定稿，立场问题在源头解决 | VOX 环节无法改立场（硬约束） | chupian-vox 规则 |
| **跨产物审查**：固化前子 agent 逐字追踪 01→02→03→04 文本/立场/数字/标题链 | 单产物复核查不出跨产物漂移 | 验收流程 |

## 2026-08-15（续）

| 决策 | 理由 | 影响 |
|---|---|---|
| **程序化边界检查**（能程序化就程序化，硬需求） | 用户硬需求：需要语义理解的才用模型 | check_chain.py 新增 |
| **跨产物一致性程序化**：01→02 文本链（含删除句剔除）、02→03 拼接、素材数字链、标题一致性、04 模板结构（核心创作任务原文比对） | 全部是确定性检查（文本匹配/子序列/正则），之前错误地交给子 agent 抽查 | 子 agent 不再查这些，只查真语义（立场/无杜撰/知识点/视觉落点） |
| **check_chain 开发过程抓到 4 个正则坑**（记录防复发）：① `\s?` 匹配换行导致跨行吞「> 」→ 用 `[ \t]?`；② 03 正文不是 > 引用块（在 **正文：** 标记后）；③ 删除句需从 01 剔除后再做子序列比对；④ 数字提取允许「数字 单位」空格 | 校验器自身也要测试（TDD） | 已修 + 56 测试覆盖 |

## 教训（L 系列，源自实测）

- L1 尺子/门框翻车：意象必须从议题推导，模板只约束结构——先挑动作再反推装置 = 模板驱动
- L2 冒号×8 漏网：立场重写也适用成稿禁令（重写规则没带禁令 = 丢规则）
- L3 复核维度不一致：子 agent 检查清单必须固定写死（漂移检查 ≠ 合规检查）
- L4 名词列举误报：长列举判据要区分动宾短语 vs 名词清单（关节/算法以动词字开头）
- L5 hook 杜撰细节：hook 只基于正文已有信息（「三个回合」教训）
- **L6 全文确认 = 没意义（反复犯，已三次）**：关卡确认时把大段产物文字（39 镜/40 镜/定稿全文）摊给用户看并问「行不行」——用户不可能读，确认 = 决策不是阅读。实证：关卡 5 用户明说「这么多文字我怎么可能去看呢」。规则：交付确认一律走决策摘要（只列 🔴 需决策项 + AI 建议 + 判断标准，用户只拍板）；需要 AI 判断的不要裸抛给用户（如「贯穿装置要不要」必须带三条判断标准）；产物全文细节放复核记录块供查阅，不弹给用户

## 2026-08-15（续·关卡5）

| 决策 | 理由 | 影响 |
|---|---|---|
| **关卡 5 拆镜作业单三层校验落地**：check_jobcard.py（镜数/时长档/超长句档位充足/参考图数量/image_prompt 对应/贯穿装置节/复核块）+ 子 agent 逐镜核对表 + 用户决策摘要 | 关卡定义预排「程序待扩展」，本轮补齐 | 测试 56→69；run_all 识别 05 |
| **时长档 = 「念得完」的最小档，不可再压**：≤18字→4s、19-27→6s、28-36→8s、>36→10s（读速 4.5 字/s） | 压档 = 念不完，后期变速 >5% 不自然（LESSONS E1） | 05 成片 4分14秒；要压只能回关卡 3 删内容 |
| **23 镜（48 字 ≈10.7s）拆 23a/23b**：拆句是切分不是改写，narration 逐字边界不违例 | 48 字超出 10s 档（Flow 10s 实际 ≈9-10s） | 05 共 41 镜 |
| **子 agent 抓出装置颜色画反（✗ 5% → 打补丁）**：4a 的 219.23 倍应青蓝（市场侧）、38.56 倍应墨黑（现实侧）；17 参考图「219 年」改青蓝 | 颜色归属 = 语义（墨黑现实/青蓝市场），自写自审有确认偏误，必须独立子 agent | 装置节已注明 H 镜唯一折中（219 倍作疑问侧墨黑） |
| **确认负担治理**：05 确认项改为「AI 建议 + 用户只拍板」，每项带判断标准 | 40 镜全文确认无意义（用户不会读），确认 = 决策不是阅读 | 关卡 5 确认模式固化 |
| **对抗性检查抓出 P1-P4（2026-08-15，第一性原理回查关卡 5）**：P1 run_all 从未调用 check_chain（README 宣称含，代码没有，跨产物链只在验收时单独跑过一次）；P2 03 假通过（段落文本无镜行格式，check_storyboard 解析 0 镜 = 0 检查 = pass；03 的正确校验是链层 02→03 拼接）；P3 03→05 narration 逐字链未程序化（只靠子 agent 口头确认）；P4 拆句阈值断层（分镜稿允许 60 字 ≈13s，Flow 档位 >36 字需 10s、>45 字必拆，48 字镜到关卡 5 才发现念不完） | 机制宣称 ≠ 代码事实，回查必须看执行路径而非 README；「0 检查 = 假通过」与「校验器自身被测试」同级对待 | P1: run_all 接入 check_chain（03 改走链层）→ P2: check_storyboard 解析 0 镜报错（防假通过）→ P3: chain_03_05（H 行=03 hook，其余=03 正文，程序验证）→ P4: check_storyboard 新增「建议拆句>45字」warning 前移拆句决策。测试 69→77 |
| **命令架构决策（2026-08-15）：不要 cover2/3/4/5** | 命令的本质 = 把控制权交给谁。阶段 1（0-5 关卡）内容全由 AI 生成，控制权在 AI 编排 → /cover 合理；阶段 2+ 流程控制已在程序（voxvideo 门禁状态机 status/resume），AI 只剩 3 个内容节点，再建 AI 命令 = 第二套控制逻辑必然漂移 | 阶段 2 直接用 voxvideo cli + START 接续步骤，不建新命令；总协调放程序（阶段 4 run-video 脚本），遇到 AI 节点停下来 |
| **阶段 3 计划确认（2026-08-15，用户口头计划 + 知识文件存档）**：①TTS 配音（voxvideo synthesize-narration 已有，Qwen3-TTS 本地/Fish 备选）②Flow 生成视频 = Chrome DevTools MCP（工具层，隔离配置文件 + 只读先行 + 3 镜试点 + 用户确认兜底）+ flow-generate skill（能力层，工作规则固化）③WSL+ffmpeg 合并脚本（LESSONS H1-H6）；④总协调 = 程序脚本 run-video 串全部 | 三阶段全是工具/程序/脚本，无「AI 内容生成 + 人工决策」新关卡 → 不需要 cover 系列；Flow 环节首次突破「AI 不碰浏览器」边界，安全规则必须进 skill | 后续开发按此执行：MCP 配置 → flow-generate skill → 合并脚本 → run-video 总协调 |

## 2026-08-17（5 问答复定案，吸纳方案修订）

| 决策 | 理由 | 影响 |
|---|---|---|
| **阶段 4 不开源（2026-08-17 用户定）**：GitHub 公开仓库化取消，改私有/本地归档 | 用户明确"不开源了" | srt-vox 素材复制风险降级：不公开分发；仍标注来源（srt-vox-director）+ 敏感处表达略改；**取消开 issue 问授权**（从前置移除） |
| **校验策略定案：他们的标准 + 我们的程序**：程序校验优先不变（TDD/豁免机制/run_all/checks-tests 骨架全不动）；srt-vox 的 16 项判据（差值≤1.0/尾部契约档位/展示型逐字覆盖/载体不撞形等）作为"标准来源"**翻译进** check_jobcard/check_stage2，不搬他的脚本（他的脚本解析 markdown 分镜表，结构不同直接搬必打架） | 用户：坚持程序校验优先，但 srt-vox 的标准来源比非科班写得好；骨架我们的+判据他们的 | 落地路径不变：映射表 → 判据翻译 → 测试 → 对抗 → loop |
| **正典一处原则确认（2026-08-17）**：受管数值（档位/尾部/混音比例）只允许出现在正典文件，check_docs 第 5 轮扫描 | 用户 ok | 先立原则再上扫描 |
| **vendoring 同步机制确认（2026-08-17）**：srt-vox 副本纳入 sync_skills EXTRA_SOURCES + hash 检测 | 用户 ok；上游 2026-08-15 刚更新，迭代快 | 与 sync_skills 目录级比较修复（旧待办）一起做 |
| **验收清单升级确认（2026-08-17）**：review-images 按参考图八项+视频六项（MiMo 提示词按维度写） | 用户确认"这个是对的" | 现成资产直接吸收 |

## 2026-08-17（出片实测 + srt-vox-director 吸纳定案）

| 决策 | 理由 | 影响 |
|---|---|---|
| **出片链路实测通过（2026-08-17）**：merge-video.py 合并 5 镜成片（25.52s 720P）+ whisper 句级字幕烧录 + MiMo 抽帧验收全部通过 | 用户手动下载 1-5 镜片段实测，全链路（合并→配音对齐→字幕→验收）可用 | 阶段 3 ③ 号脚本实战验证；字幕链路（WSL faster-whisper + ASS + 烧录）首次跑通，补 LESSONS 坑：ffmpeg 单帧输出 -update 1、模型路径用仓库名、pip --break-system-packages |
| **吸纳 srt-vox-director（2026-08-17 用户定案，分两步）**：第一步文字部分（16 条文本规则+规则库结构，程序先行+子 agent test 托底+对抗性测试+返工 loop）；第二步图片部分（21 风格资产，Agnes+MiMo 产线自造，之后做） | 它的问题形态与我们的哲学同构（文档正典+校验器+人工验收）；真缺口=补差、尾部契约分档、差值显式化、两型、桥接帧、check_docs、强制试点 | 轮次表 0-6 落地（见 PROGRESS.md 吸纳计划）；先做前置：版权问询/检查项映射表/正典一处/vendoring |
| **就近吸附取代向上取整（第 2 轮）**：flow_slot 从"念得完的最小档"改为就近吸附（分界 5/7/9），硬规律「自然时长≥3s ⟹ 差值≤1.0s」 | srt-vox 实测论证：向上取整白多 1.1s 要编收束动作；就近吸附差值 ≤1.0s，剪辑一步处理 | 前置依赖：Flow 实际成片时长统计（第 0 轮）——D1 矛盾（8s 档 7.23s vs 10/6/4 档整秒）未解前不改吸附 |
| **尾部契约表分档（第 1-2 轮）**：统一"结尾稳定 0.8s"改为按差值分档（余量≤1.0 静止/1.0-2.0 收束/补差末帧定格）；补差检测进 merge-video | 差值 >1.0s 统一 0.8s 静止 → 模型空段自行发挥；视频短于旁白时裁尾无效（真缺口） | 提示词模板改 + merge-video 警告 |
| **垫图认知入 LESSONS（第 1 轮）**：上传图是垫图/目标构图，模型重新生成第 0 帧，生成片第 0 帧 ≠ 上传图 | 用户曾困惑"生成内容不是提示词内容"，部分就是垫图机制 | 防误判生成结果；参考图提示词画目标构图不是待填底板 |
| **强制前段试点（第 6 轮）**：每期拆镜后强制先出前 8 镜 + 1 参考图 → 用户确认方向 → 全量（不按 >40 镜触发，方向验证与规模无关） | 提示版升级为强制版；与 flow-generate 3 镜试点形成两级（提示词级+生成级）；方向错返工从 41 镜缩到 8 镜 | 每期多一轮确认，符合"每关独立交付独立确认" |
| **check_docs 全套要（第 5 轮）**：受管数值扫描（档位/尾部/混音比例正典一处）+ 规则实现覆盖（P6 防复发）+ 断链/引用位 | 用户拍板：规则库正在膨胀（吸纳后达 8 万字级），P6 前科（上游规则漏翻译）证明漂移真实存在 | 前提：正典一处原则先立（本文件记）再扫描 |
| **检查项映射表前置**：srt-vox 16 项 vs 我们的 check_jobcard/check_stage2/check_chain 逐项对账（已有/新增/冲突），防两套标准打架 | 吸收前先对账，否则同一产物两个判定结果，子 agent 无所适从 | 第一轮工作先做映射表再动校验器 |
| **版权处理**：仓库无 LICENSE（默认保留权利）→ 先到 repo 开 issue 问授权；期间"结构全收+表达重写"，不逐字复制 | 阶段 4 要开源，逐字复制资产带法律风险 | 授权结果决定文本层吸收深度 |
| **验收清单升级**：review-images（MiMo 看图）从"风格可用/乱码/损坏"升级为参考图八项+视频六项（画幅/目标构图/主体数量/文字/连续性/漂移/无运动模糊/形状完整…） | 现成资产，比我们现状细，直接可吸收 | MiMo 看图提示词按维度写 |

## 2026-08-17（吸纳 srt-vox 文字部分·轮次 0-2 落地）

| 决策 | 理由 | 影响 |
|---|---|---|
| **第 0 轮结论：Flow 档位整秒假设成立，7.23s 是孤例**：宇树 5 镜实测 4 档 4.01s / 6 档 6.02s / 8 档 8.00s，差值全为余量（0.26-0.90s）。merge-video 自动记录档位 vs 实测（.work/flow-duration-log.json，每期累积） | 2026-08-11 的 7.23s 与后两期全部整秒矛盾；差值算术前提 = 档位名义时长（误差 <0.1s 可忽略） | 就近吸附规则落地（第 2 轮）；flow-generate LESSONS E1-E3 |
| **check_stage2 时长链语义修正**：design.duration_seconds = 配音实测（任意正数），不再是档位值——旧校验把实测当档位假报 41 条（宇树真实产物暴露） | synthesize-narration 写回实测后校验器没跟上 = P6 同类静默漂移 | b 项重构：正数校验 + 05 档位 vs 就近吸附偏差进 stats.stale_slots（05 是配音前估算，不算违规） |
| **handoff 时长解析正则修复**：旧正则匹配「建议时长」对真实产物（「Flow 档位：6s（旁白真实时长 5.8s）」）0 匹配 → 时长校验静默失效 | 宇树 handoff 43 处「Flow 档位」0 处「建议时长」，校验器在空转 | 新增 HANDOFF_FLOW_RE 主格式 + 旧格式兜底；新增「Flow 档位 == flow_slot(实测)」一致性校验 |
| **补差尾部检查查物理要求不查措辞**：补差镜要求尾部含「稳定/静止/定格」（物理：末帧会被定格延长），不强制「末帧」字面——「结尾稳定保持 0.8 秒」已满足（0.8s ≥ 0.2s 静止），不误报旧句式 | 判据翻译原则：程序查可验证的物理事实，剪辑提示语（这一帧会被定格延长）属文档规则 | check_stage2 尾部契约分档（余量/补差两路）；宇树 32 条误报消除 |
| **第 2 轮对抗性测试通过（宇树 IPO）**：新校验器 0 issues，差值率 11.76%（≤20% 正常）；handoff 按新 flow_slot 重新 export（旧产物对齐新规则的标准动作，非豁免） | 就近吸附取代向上取整 = 规则变更，旧 handoff 重导即可 | 测试 121→138；宇树 run_all --project all_pass |

## 2026-08-15（阶段 3 启动）

| 决策 | 理由 | 影响 |
|---|---|---|
| **chrome-devtools MCP 配置落地**：opencode.jsonc 加 mcp.chrome-devtools（npx chrome-devtools-mcp@latest，127.0.0.1:9222）+ 隔离 Chrome 启动脚本 scripts/chrome-flow.bat（%USERPROFILE%\.chrome-flow-agent） | flow-knowledge.txt 方案执行；隔离配置文件 = 安全底线（Chrome 官方提醒：MCP 可读 Cookie/LocalStorage） | MCP 配置就绪，待启动 Chrome + 只读测试验证连通性 |
| **flow-generate skill 建立（2026-08-15）**：把 flow-knowledge.txt 的阶段 3 相关部分（只读测试 / 18 条工作规则 / 3 镜试点 / 安全设置）沉淀成全局 skill（~/.config/opencode/skills/flow-generate/），已加白名单 + sync 进项目快照 | flow-knowledge.txt 是「用法记录」不是「可执行规则」，skill 化后每次会话按规则走，不靠重读长文；Flow 环节突破「AI 不碰浏览器」边界，安全规则必须进 skill 固化 | 阶段 3 第 ② 项完成；后续 Flow 生成会话由 skill 触发，按 只读先行→3镜试点→用户确认兜底 执行 |
| **ffmpeg 合并脚本 merge-video.py（2026-08-15）**：逐镜视频裁尾对齐旁白(H3, -t 旁白实测时长) → concat 拼接 → 混音(H6: 视频音效 0.6 + 旁白 1.5, amix normalize=0 防减半 N13)。支持部分镜头已生成（只拼已存在视频）；旁白时长用 ffprobe 实测优先、design.json duration_seconds fallback | LESSONS H1-H6 + N13 的合成链路规则要程序化，不靠每次手动拼 ffmpeg 命令；冒烟测试 2 镜跑通（9.16s ≈ 旁白 9.12s） | 阶段 3 第 ③ 项完成；踩坑：-c:v copy 与 filter_complex 冲突（视频过 null filter 就不能 streamcopy）→ 视频直接 -map 0:v copy、只音频过 filter |
| **Flow 提示词输入方式（2026-08-15 实测，B1-B5 沉淀）**：type_text 多行输入 = 每段独立提交（灾难，耗点数）；剪贴板粘贴（Set-Clipboard → Ctrl+V）= 完整单段；参考图引用 = 输入框 @ 触发媒体选择（起始/结束帧位是首尾帧另一套功能，拖拽与 MCP 不兼容） | 实测踩坑：第一次 type_text 输入 350 字多行提示词 → Flow 按空行分段生成 6 个失败项 + 耗点数；剪贴板粘贴 350 字一次成功无提交 | flow-generate LESSONS.md B1-B5；后续所有镜头输入走「@引用 + 剪贴板粘贴」 |
| **sync_skills.py 只比较 SKILL.md（2026-08-15 发现）**：LESSONS.md 等附属文件变更检测不到（hash 只对 SKILL.md）→ 手动复制同步 | 实测：flow-generate 加 LESSONS.md 后 sync 报 same | 待修：改为目录级比较或 hash 清单（记入待办） |

## 2026-08-15（阶段 2 实测后）

| 决策 | 理由 | 影响 |
|---|---|---|
| **review-design 门禁阈值实证**：子 agent 逐镜复核异常率 7.3%（3✗/41）< 20% → 修完 ✗+⚠ 后标记通过 | 05 关卡 5 先例同款（5% 打补丁）；门禁判据「异常<20%」第一次在真实项目跑通 | 门禁流程固化：子 agent 复核 → 修复 → 重验（相邻同底 NONE）→ review-design |
| **阶段 2 全链打通**：init → design.json → review-design → generate-images → approve-images → export-handoff（20260815-unitree-ipo） | 真实项目实测 0-5 产物 → voxvideo 素材包全流程可用 | 后续每期视频直接走此链路；阶段 3 自动化（05 表格→design.json 生成器）提上日程 |
| **阶段 2 对抗性回查 P5-P8（2026-08-15）**：P5 05→design.json 无程序校验器（narration 逐字/时长/参考图/image_prompt 全靠子 agent 口头——P3 同类问题在阶段 2 复现）；P6 handoff vs design 无校验；P7 run_all 不覆盖阶段 2；P8 manifest 状态 vs 产物文件无校验 | 机制宣称 ≠ 代码事实：阶段 2 产物没有校验器 = 只有语义层没有程序层，与信任链第一性原理冲突 | 新增 check_stage2.py（05→design→handoff→manifest 全确定性检查）；run_all --project 一键全链；测试 77→109；run_all 集成测试防 P1 复现 |
| **阶段 2 子 agent 语义托底结论（4 ⚠ 无 ✗）**：修 2 处——29 镜「219 倍」改墨黑（05 装置节「呼应 H」结构镜，H 是唯一墨黑例外，29 同构疑问侧）、23 镜「新能源汽车」改青蓝（未来读法=答案侧，与 4b/14 跨镜一致）；记录 2 处——32 镜 05 内部动作要点（答案卡）vs image_prompt（指向画外）不一致被继承（design 忠实 image_prompt 原文）、2 镜三卡全青蓝压平强调层次（150.80/610 跨镜自洽优先） | 程序查不了颜色归属（语义），但颜色跨镜自洽可以查——留给子 agent 是边界内决策 | 29/23 修复后重跑 generate-images 全 skipped（D4 幂等）+ approve + export，stages 恢复 |
| **⚠ 待决（用户提出，未决策）**：① 项目文件归位——阶段 1 产物（01-05，现散在 Desktop\test\）应与阶段 2 项目文件放在一起（归入 `projects/20260815-unitree-ipo/`），放 test 只是当时临时要求；用户觉得 C 盘（Default Project）存放不合适，最终位置待定；② 识图 API 配置项化——MiMo 欠费 402 时不得阻塞，做成配置项：AI 看图 / 问使用者 / 用户自己识别三选一；③ 生图配置项化——可选手动生成（不依赖 Agnes）；④ 生图+识图都可全手动 | 实测踩坑：MiMo 402 欠费导致 approve-images 阻塞，只能临时让用户人工看图；生图同理应允许手动 | 下次开工前处理配置项；存位置迁移时注意 handoff.md 相对路径（LESSONS F1） |
| **子 agent 复核明细级留痕 = 全关卡规则（2026-08-15，用户明确定位）**：不是历史备注，是新文案/新话题的规则——每关子 agent 复核必须落盘明细级核对表文件（逐项判定+异常处置），摘要级（只在复核块写结论）不是允许的完成形态 | 用户追问「子 agent 复核一定有留痕吗」暴露 01-04 只有摘要级；对话会丢，文件不会；复核可验证性是三层信任链的语义层地基 | 关卡定义.md 通用硬规则第 5 条 + START.md 速记同步；01-04 规则确立前历史产物不追溯，维持摘要级 |
| **01-04 重新复核结论（2026-08-15）**：02/03/04 通过（⚠ 说明性）；**01 发现 3 处翻案腔**（human-writing 硬禁令「不是…而是…」：L15 引用块内 + L15/L19 正文）→ 用户决策**豁免登记** | 素材原句直引 + narration 已冻结（02/03/05 逐字链）+ 规则确立前产物；修全链 = 破坏 05 已确认产物，成本远大于收益；防再犯靠程序补漏 | 01 稿复核块加豁免登记行；check_rewrite 新增翻案腔检测（8 测试）；run_all 新增豁免机制（登记项降 exempt，可见可查非沉默放行） |
| **豁免机制设计（2026-08-15）**：产物文件内「豁免登记：<检查项>」标记 = 用户决策落盘，run_all 将该检查项从违规降为 exempt（报告仍列出 + severity=exempt 可见）；只豁免明确登记的项，其他违规照常报 | 程序报错 → 用户决策 → 豁免必须可查（防 P1 同类：沉默放行 = 没有豁免） | run_all.check_file 实现 + 4 测试；历史产物可用，新产物原则上不豁免（规则确立后按规则走） |
| **05 时长表笔误修复（2026-08-15）**：21 重复出现 2 次（42 组 vs 41 镜）→ 第 1 行第 3 组 21 改为 22 并整体下移，末行补占位；04「柔和阴影」皮肤残留（默认瑞士平面措辞混入 minimal-motion）→ 改「无阴影」×2 处 | 子 agent 02 复核抓出（程序 check_jobcard 只查主表不查汇总表）；04 子 agent ⚠1 抓出 | 05 时长表 41 组 282s 与头部自称一致；04 皮肤纯度恢复 |
| **项目位置规则（2026-08-15 用户定）**：项目文件放一开始指定位置；未指定必须问用户，不得默认。阶段 2 项目已按用户决策移到 `C:\Users\xx\Desktop\test\20260815-unitree-ipo\`（与 01-05 同处） | 用户：项目位置是要问的决策，不是 CLI 默认值；上次 init 默认放 Default Project 没问 = 流程缺陷 | voxvideo CLI 新增 `VOXVIDEO_PROJECTS_DIR` 环境变量（默认 ROOT/projects 不变，90 测试全过）；今后 init 前先确认位置；迁移后 handoff 相对路径仍有效（项目内相对） |
| **配音后端顺序（2026-08-15 用户定）**：Qwen3-TTS 优先（本地克隆声）→ MiMo TTS 备选（token-plan，TTS 限时免费）→ Fish 兜底 | 用户明确优先级；MiMo TTS 实测 24kHz 音质可用 | config.narration.provider 三选一：qwen3-tts（默认）/ mimo-tts / fish-audio；MimoTtsNarrator 实现（assistant=文本、顶层 audio 参数、message.audio.data 取 base64 wav），测试 90→97 |
| **配音后端最终定案（2026-08-15 用户试听后改）**：**MiMo TTS 置顶优先**（provider=mimo-tts），克隆声降级备选，Fish 兜底 | 用户试听 narration-mimo.wav（语速明快 3:11）后拍板用小米；实测「沉稳专业语速适中」style_prompt 反而拖慢 23%（8.64s vs 无 style 7.04s），「明快干脆语速偏快」最快（5.76s） | config style_prompt 改「明快干脆」+ provider=mimo-tts；narration.py 修配置段 bug（mimo_tts 在顶层 config 不在 narration 段，导致 narrator_from_config 取到空 style_prompt）；旧 key（sk-cw70tbt…）全局清除（用户级环境变量+残留文件） |
| **字幕链路顺序（2026-08-15 用户定）**：whisper 转写优先（WSL）→ MiMo ASR 备选（token-plan，按量计费非免费） | 用户明确优先级；MiMo ASR 实测转写准确（chat/completions + input_audio，不能带 text 部分，asr_options.language） | mimo_asr.py 脚本固化（wav/mp3 ≤10MB，zh/en/auto）；ASR 按量计费，注意余额 |
| **MIMO key/URL 读取规则（2026-08-15 实测教训）**：.env 优先于环境变量；URL 固定 token-plan-cn.xiaomimimo.com/v1（禁用 MIMO_BASE_URL 环境变量） | 实测踩坑：shell 环境变量残留旧 key（sk- 前缀 api.xiaomimimo.com）与旧 URL，导致 401/402 反复，脚本读 .env 的新 key 也被环境变量覆盖 | 三个脚本统一：.env 读 key + URL 硬编码新地址；mimo_asr.py MODEL 固定 mimo-v2.5-asr（MIMO_MODEL 环境变量残留 mimo-v2.5 是坑） |

## 2026-08-17（第二步：21 风格资产自造）

| 决策 | 理由 | 影响 |
|---|---|---|
| **21 方向映射定案（3 重合升级 + 18 新增注册）**：S07 极简几何→minimal-motion、S08 水墨长卷→ink-scroll、S11 产品蓝图→blueprint-craft 归并（补样图/风格板引用，不重复注册）；其余 18 个方向新注册；vox/fresh-scrapbook/warm-illustration 与 21 方向无重合保持原样（vox 是剪辑感纸艺舞台、S10 是考古实物桌面，不同世界） | 判据 = 视觉世界同义（材质+配色+空间语言+运动逻辑整体重合），不是只看名字或单维配色；重复注册 = 同一方向两个 guide 漂移风险 | docs/style-mapping.md；24 风格注册齐 |
| **产线改 runninghub（用户决策）**：Agnes 首张 S01 达标，但用户判断系统 Agnes 平台不稳 → 样图/风格板全部改用户 runninghub 生成（AI 出整份提示词 MD 到桌面 → 用户生成 → 按 1-21 顺序存 → AI 改名） | 出图质量依赖平台稳定性，用户自选平台更可控；AI 产出文本（提示词/验收/评级）不变 | 样图 21/21 中文逐字正确；Agnes 脚本保留（幂等可续），后续出图可二选一 |
| **样图验收误报口径定案**：参考图八项维度 3（主体数量）/5（连续性）是为单镜参考图设计的——样图本就四件物件、静态无锚点，MiMo 按模板口径报不合格 = 口径不符非图的问题；画幅误报用 PIL 读实际像素复核 | 模板与场景口径不匹配会造成假失败，逼用户重抽无意义的图 | style-gallery.md 验收说明节 + LESSONS Q2；样图按「四件齐全无多余」判定 |
| **风格板验收六项（独立模板）**：六格结构/标签字/载体空白/材质统一/形状完整/画幅——与参考图八项分开，不混用 | 风格板读者是图像模型，无「主体唯一/转场锚点」概念，混用模板必然误报 | scripts/review-style-boards.ps1 + LESSONS Q3 |
| **check_docs 扫描范围修复（对抗性回查抓出 2 个空转）**：① docs/*.md 不递归子目录 → docs/style-assets/ 三个新文档漏扫 → 改 docs/**/*.md；② Default Project 风格文档（ref/ + config/styles/）不在扫描范围 → 加 EXTRA_STYLE_ROOTS 外部根 + 路径缺失报 issue 防静默跳过 | 「新增资产后必须跑 check_docs」若扫描范围没覆盖新资产 = 联动空转（P6 同类：机制宣称≠代码事实） | 测试 165→167；guide/master 尾部句式受外部根扫描管辖 |
| **尾部句式统一**：18 新 guide 全部「结尾按尾部契约选档（余量档静止收束/补差档末帧定格）」；5 个现有 guide 旧「0.8 秒」句式各 2 处已替换 | 正典一处：尾部数值只活在 check_stage2，风格文档只准引用 | 5 guide 更新 + 新 guide 模板 |

## 2026-08-18（成片阶段流程修复）

| 决策 | 理由 | 影响 |
|---|---|---|
| **成片阶段强制流程**：chupian-vox SKILL.md 新增「成片阶段（强制流程）」章节，明确触发条件（用户要求合成视频/出成片/合并MP4）和完整链路（裁尾对齐→拼接→混音旁白→Whisper 转写→ASS 字幕烧录→导出 final-subs.mp4） | 新对话启用 skill 时没有先读 LESSONS.md，导致用户要求"合成视频"时简化为单纯视频拼接，漏掉关键步骤。SKILL.md 只定义"到导出 handoff 为止"，没有写成片流程。LESSONS.md H1 是经验记录，不是强制流程 | 每次启用 skill 都会读到成片流程，不依赖经验记录；用户要求出成片时执行完整链路，不再简化 |
| **边界部分重构**：原「边界」只写"AI 不参与"，改为区分「素材包阶段」（AI 完成 handoff 后收工）和「成片阶段」（AI 按强制流程执行） | 原边界描述过于绝对，导致 AI 在用户要求出成片时仍坚持"不参与"。需要明确两个阶段的不同职责 | SKILL.md 边界部分更清晰，AI 知道何时该执行成片流程 |
