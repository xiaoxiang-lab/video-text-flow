# 校验器设计说明

## 判据（普适，不随领域变化）

> 某项检查能否形式化为「输入 → 确定性规则 → 判定」？
> 能 → 程序；不能（需要理解含义/意图/跨句语义）→ 子 agent。

程序 = 翻译一次、执行 N 次（零方差）；LLM = 每次重新解释（方差 = 漂移）。
**能程序化就不要大模型**——这是原理，不是偏好。

## 规则假设来源标注（规则 = 假设，不是真理）

每个参数标注来源，迭代时知道改哪个、影响什么：

| 规则/参数 | 值 | 来源 |
|---|---|---|
| 单句 ≤60 字（≈9s） | 60 | 实测：模型单段上限 10s，vox-director 蒸馏（2026-08 实战） |
| 残句线 <12 字 | 12 | 实测：子 agent 复核案例（衔接词起头超短残句） |
| 条件从句独立成镜（如果/因为/当） | — | 实测：32a 案例（话没说完） |
| 提示性冒号 | — | 偏好：human-writing 成稿禁令 |
| 引语冒号豁免（说/写道/表示） | — | 偏好：成稿禁令原文例外 |
| 破折号 | — | 偏好：human-writing 成稿禁令 |
| 同构排比 ≤2 项 | 3 命中 | 偏好：成稿禁令（两项为限） |
| 长列举 ≥6 项且 >20 字 | 6/20 | 实测：口播单口气可念线 |
| 设问镜连续 ≥3 | 3 | 偏好+实测：类型红线（定义/论证链豁免） |
| 排版参数内置（画幅/占比/断行/强调/无其他文字） | 分类 | 实测：生图模型只看提示词正文（用户实测翻车） |
| 解码动作内置（隐喻说明句） | — | 实测：模型不理解「为什么这么设计」（用户实测） |
| 封面禁止词 | 词表 | 偏好：视觉总监模板禁止清单 |
| 关键事实保留核对 | 调用方传 | 偏好：立场重写规则（事实不丢） |

## 程序 vs 子 agent 边界（按判据推导）

**程序**：字符统计、正则模式、词表、计数、格式、结构（含启发式近似：设问句、过渡句模式、排比模式、数字密度）。

**子 agent（受约束）**：视觉落点、立场一致性、一句一知识点（本质语义，程序只近似）、来源标注真实性、类型豁免裁决。

**子 agent 的约束**：
1. 范围收窄：程序跑完才轮到它，只查程序报告之外的语义项
2. 清单自拉：读 `checks/checklist-*.md` 固定文件，不由主 agent 转述
3. 输出结构化：判定输出为程序可解析格式，程序验证其完整性（防子 agent 漏项）

## 已知小瑕疵（记录）

- 破折号「——」同时命中「——」与两个「—」（3 条记录同一位置）——去重待加
- check_rewrite 的 COLON_LEADERS 词表需要随语料扩充（词表驱动，普适）
- check_storyboard 的短镜豁免表（便宜/为什么/没错）是案例驱动的，新语料遇到新短镜要补
